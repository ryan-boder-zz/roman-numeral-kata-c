#ifndef FROM_ROMAN_H
#define FROM_ROMAN_H

/**
 * Converts a roman numeral to an integer.
 *
 * The caller is responsible for ensuring that the input is a valid roman numeral.
 */
int roman_numeral_to_integer(const char* roman);

#endif
