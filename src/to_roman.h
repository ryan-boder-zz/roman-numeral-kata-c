#ifndef TO_ROMAN_H
#define TO_ROMAN_H

/**
 * Returns a Roman Numeral for the given integer or NULL if integer is invalid.
 *
 * The returned string is dynamically allocated and must be freed by the caller.
 */
const char* integer_to_roman_numeral(int integer);

#endif
