#ifndef TO_ROMAN_H
#define TO_ROMAN_H

/**
 * Converts an integer to a roman numeral.
 *
 * The caller is responsible for ensuring that the input is in the range of valid roman numerals.
 */
const char* integer_to_roman_numeral(int integer);

#endif
